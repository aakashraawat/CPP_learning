#include<iostream>
#include<cmath>

using namespace std  ;


int main ( ){

    char x = 'A'; // only one letter can be stored as they have only one byte memory


    string  S[10] = "Hello " ;// array of char to make a string  null character =(\0) to end the array with this character and always used with strings array

    char S[] = "Hello" ;

    char S [] = {'H','E','l', 'l' ,'o','\0'} ;// string always ends with \0

    // using pointers to store in heap memory 

    char *S = "HEllO"






}